# 要件定義と自己評価

会話全体で確定した要件を整理し、現時点の到達を評価する。**確信度は根拠の種類で区別する。** 「テスト済」はNode上で純ロジックを検証したもの、「未検証」はGAS実機で一度も動かしていないもの。**本システムは実機で一度も起動していない。**

---

## 1. 要件定義

### 1.1 目的

Google Workspace が構造的に埋められない穴を埋める。当初は「国語の縦書き」だったが、より上位の穴が特定された。

> **学級全員分の定型テキストを、教師が1操作で紙面化・集約・提示できること。**

縦書き・マス目・ルビはその一部にすぎない。ClassroomもFormsも、35人分をまとめて紙にすることと、全員の同じ欄だけを抜き出して並べることができない。

### 1.2 優先順位（衝突時の規準）

1. **使用時の認知的負荷の低さ・使い勝手**
2. 機能性
3. 実装の容易さ

実装が複雑になっても、使うときのややこしさを削るほうを選ぶ。

### 1.3 制約

| 項目 | 内容 | 出所 |
|---|---|---|
| 基盤 | Google Workspace for Education | 前提 |
| GAS | 使用可 | 前提 |
| Classroom API | **使用不可（確定）** | 環境で確認済 |
| Marketplaceアドオン | 使用不可 | 環境で確認済 |
| 端末 | Chromebook 1280×800、メモリ4GB／8GB | 前提 |
| 同時利用 | 1学級35人 | 前提 |
| PDF生成 | GASでは不可能。ブラウザ印刷のみ | 技術的確定 |

### 1.4 機能要件

| ID | 要件 | 段 |
|---|---|---|
| F1 | ホーム→教科→単元→授業→ページの階層 | 1 |
| F2 | 教師がオブジェクトを配置してワークシートを作る | 1 |
| F3 | 児童が自分の複製に書き込む（共同編集ではない） | 1 |
| F4 | 集約対象の入力欄をスプレッドシートの列にする | 1 |
| F5 | 入力欄の種類はFormsに準拠 | 1 |
| F6 | 単元1ファイル・授業1シート | 1 |
| F7 | 授業シートが回答の正本 | 1 |
| F8 | 縦書き原稿用紙（ルビ・行頭禁則） | 1 |
| F9 | 改名・削除・並べ替え | 1 |
| F10 | JSONの取り込みと書き出し（往復） | 1 |
| F11 | 全員分を1人1枚で印刷 | 2 |
| F12 | 縮小面付けで掲示用に印刷 | 2 |
| F13 | **特定部分だけ抽出して集約し、紙面化・投影** | 2 |
| F14 | 位置集約（付箋の座標を重ねる） | 3 |
| F15 | 児童による移動・複製・削除 | 3 |
| F16 | テンプレートの版凍結、単元コピーの自動化 | 4 |

### 1.5 非機能要件

| ID | 要件 |
|---|---|
| N1 | 35人が同時に書き込んでも詰まらない |
| N2 | 新しい入力欄の追加が1箇所で済む |
| N3 | コード変更なしで新しい単元・授業を作れる（属人化対策） |
| N4 | 教師以外が全児童のデータを見られない |
| N5 | 児童が書いた内容を失わない |
| N6 | 改修で壊れたことに気づける（自動テスト） |
| N7 | 児童のページは縦横スクロールしない |
| N8 | Classroomへの投稿は年1回で済む |

### 1.6 範囲外（明示的に却下）

画像・音声の提出、手書き描画、自由レイアウト、共同編集、汎用スライドエディタ、成績処理。いずれもClassroomや既製品が担うか、定型性を壊して優位性を失わせる。

---

## 2. 到達の自己評価

### 2.1 機能要件

| ID | 状態 | 根拠と確信度 |
|---|---|---|
| F1 | 実装 | 未検証（実機起動なし） |
| F2 | 実装 | 未検証。座標計算はテストなし |
| F3 | 実装 | 未検証 |
| F4 | 実装 | 列展開・復元はテスト済／書き込みは未検証 |
| F5 | 実装 | 8種。テスト済（描画・既定値・設定項目） |
| F6 | 実装 | 未検証 |
| F7 | 実装 | 未検証 |
| F8 | 実装 | 組版はテスト済（ルビ分割・禁則・改行） |
| F9 | 実装 | 未検証。改名は3箇所同時更新 |
| F10 | 実装 | 正規化・自動配置・往復はテスト済 |
| F13 | **実装（この回で追加）** | 未検証。抽出・並べ方・投影・印刷 |
| F11・F12・F14〜F16 | 未着手 | — |

### 2.2 非機能要件

| ID | 状態 | 評価 |
|---|---|---|
| N1 | **不確実** | 行の事前確保で排他制御は不要にした。ただし**GASの同時実行数上限に当たる可能性を評価していない**。自動保存の間隔をばらして緩和したが、実測していない |
| N2 | 達成 | registry.html の `KIND` に1件足すだけ。ツールバー・既定値・描画・設定・自動配置・集約列・取り込み案内がすべて追随する。テスト済 |
| N3 | 達成 | 単元・授業の作成、テンプレート編集、取り込みのすべてが画面から可能。コードを触る必要がない |
| N4 | 実装 | サーバ側の教師照合を全操作で通している。**未検証。ここが最も重い** |
| N5 | 部分 | 自動保存・未保存警告・列の保全は実装。**版履歴や復旧手段はない**（GitHubに上げればコードだけは戻せる） |
| N6 | 達成 | サーバ28件＋クライアント検証。`runTests` で随時走る |
| N7 | 実装 | A4横に固定し自動縮小。未検証 |
| N8 | 達成 | URL1本、行き先は「今日の授業」で切り替え |

---

## 3. 私の誤り・不正確だった記述

**① 「集約シートは自動更新しない」設計を、次の段で自分で否定した。** 段1で競合を避けるため手動生成にしたが、授業シート自体を正本にすれば競合しないことに、指摘を受けてから気づいた。最初の設計が回りくどかった。

**② 「即座に反映されます」は不正確だった。** 児童の保存はすぐシートに入るが、**教師がシートで直した内容は、児童が次に開いたときにしか反映されない**。開いている最中に入れ替わることはない。文書とUIの文言を訂正した。

**③ 自作の射程を「日本語組版の特殊要件」に限定したのは狭すぎた。** 一括紙面化というより大きな穴を見落としていた。指摘を受けて修正した。

**④ 拡張性の欠陥を、指摘されるまで自分から報告しなかった。** 入力欄1種類の追加に7箇所の同期が必要な状態を作っておきながら、リファクタリングを求められるまで問題として挙げなかった。

**⑤ 押し忘れを前提にした設計を作った。** 速さのために名簿をキャッシュし、「名簿から行を追加」を押さないと氏名が古いままになる状態を作った。利用者に手順を記憶させる設計は採らないという原則に反していた。指摘を受けてキャッシュを廃止し、提出画面を開いた時点で自動的に整うようにした。

**⑥ 「教師が紙面を直しても児童には最大15分反映されない」と文書に書いたのは誤り。** テンプレート保存時にキャッシュを破棄しているので即時に反映される。反映されないのはスプレッドシートを直接編集した場合だけ。訂正済み。

**⑦ 学習指導要領との整合を一度も検証していない。** 前提条件に挙がっているが、私はデータ構造とUIの話しかしていない。単元・授業の粒度が指導計画とどう対応するかを詰めていない。

---

## 4. 未検証リスク（実機で壊れる可能性が高い順）

| # | 箇所 | 内容 | 確信度 |
|---|---|---|---|
| 1 | `syncHeader_` の列操作 | `deleteColumns` → `insertColumnsAfter` の組み合わせ。`getLastColumn()` は「データのある最終列」であり実列数ではない。空シートや列数の食い違いで例外の可能性 | 壊れる確率 3〜4割 |
| 2 | `ScriptApp.getService().getUrl()` | デプロイ前に実行すると失敗する。`setUp` の段階では呼ばないので問題ないはずだが未確認 | 2割 |
| 3 | 35人同時保存 | GASの同時実行数上限。自動保存の分散で緩和したが未実測 | 授業終了時に詰まる確率 2〜3割 |
| 4 | `DriveApp.moveTo` | 単元ファイルのフォルダ移動。権限や共有ドライブで失敗しうる。失敗してもマイドライブに残る作りにしてある | 1〜2割 |
| 5 | ポインタ座標 | `transform: scale()` した要素上のドラッグ。倍率で割る計算は正しいはずだが、Chromebookのタッチとスタイラスで未検証 | 2割 |
| 6 | `importUnitBundle` | 途中で失敗すると単元だけ残る。ロールバックがない | 中断時に必ず発生 |

**最初に実機で確かめるべきは1番です。** ここが壊れると、テンプレートを保存するたびに児童の回答列が壊れる可能性があります。入力欄を1つ置いて保存し、シートの列を目視してから次に進んでください。

---

## 5. 率直な総括

**F13を実装したことで、既製品に対する優位が最小限は立ちました。** ただし未検証です。

私自身が「優位性は一括紙面化・抽出集約・日本語組版の3つ」と定義し、「段3（集約の紙面化）まで到達しない限り、既製品を使ったほうが早い状態が続く」と書きました。その基準で見ると、いまはF11〜F13が未着手で、**縦書きが使えるFormsの代わり**という位置にとどまっています。

Formsに対しては既に優位です。図と問いが同じ視野に入る、縦書きが使える、定型レイアウトが保てる。ロイロノートやMetaMoJiに対しては、まだ優位がありません。

**次はF11・F12（個票の一括印刷と縮小面付け）です。** F13で「全員の同じ欄を並べて投影する」までは届いたので、残るのは回収と掲示の側です。ただしF13が実機で動くことを確かめてからにすべきです。

**その前に、実機での起動確認を挟むべきです。** 未検証の項目が多すぎて、いま機能を積むと不具合の切り分けができなくなります。
