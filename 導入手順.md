# ワークシート集約プラットフォーム

Formsではできない「図と問いが同じ視野に入るワークシート」を作り、集約対象の入力欄をそのままスプレッドシートの列にする。

---

## ファイル構成（18）

**サーバ（.gs／7）**

| ファイル | 責務 |
|---|---|
| Main.gs | ルーティングのみ |
| Store.gs | マスター／単元ファイルへのアクセス、権限、名簿、キャッシュ |
| Schema.gs | テンプレートのスキーマ版・検証・移行 |
| Lesson.gs | テンプレート、回答、列同期 |
| Admin.gs | 単元・授業の作成／改名／削除／並べ替え／公開 |
| Port.gs | 書き出しと単元一括取り込み |
| Collect.gs | 抽出集約（取り上げの指定と読み出し） |
| Test.gs | 自動テスト（`runTests`） |

**画面（HTML。拡張子なしでこの名前ちょうど）**

| ファイル | 責務 |
|---|---|
| css | 全画面共通のスタイル |
| genko | 縦書き組版 |
| **registry** | **オブジェクト種別の定義。拡張はここだけ** |
| layout | 座標の自動配置と取り込みの正規化 |
| render | ページ描画、画面遷移、回答収集 |
| app | 児童用 |
| editor | 教師用オーサリング |
| teacher | 教師用一覧・提出状況 |
| import | 取り込み専用（`?view=import`） |
| collect | 抽出集約（`?view=collect`） |

---

## 1. セットアップ

1. script.google.com で新しいプロジェクトを作り、18ファイルを貼る
2. `setUp` を実行 → 権限を承認
3. 実行ログの2つのIDを `Store.gs` 冒頭に貼る（`MASTER_ID` / `FOLDER_ID`）
4. 保存して、もう一度 `setUp`
5. **`runTests` を実行し、全28件合格することを確認する**
6. マスターの「名簿」に A列=児童のメール、B列=氏名／「教師」に教師のメール

## 2. 公開

デプロイ → 新しいデプロイ → ウェブアプリ

| 項目 | 設定値 |
|---|---|
| 実行するユーザー | **自分** |
| アクセスできるユーザー | **組織内の全員** |

「実行するユーザー」を誤ると児童全員に権限承認画面が出る。唯一の致命的な設定ミス。

児童・共通は デプロイURLそのまま、教師は `?view=teacher`。**Classroomに貼るのはデプロイURL1本だけ。**

---

## UI設計の根拠

### 通説の訂正：「選択肢は7±2まで」は誤用

Millerが測ったのは復唱できる項目の短期記憶容量であって、画面に並んで見えている選択肢の話ではない。Cowanの推定では実効容量は約4。**見えているものは記憶しなくてよいので、項目数より分類とラベルの質が効く。**

Hickの法則も等確率・未知の選択肢に対する法則で、意味的に分類された既知のリストには弱くしか効かない。

したがって道具14個を減らすのではなく、「おく／書かせる」の2群に分けたまま、置き方のほうを直した。

### 使用時の負荷は、実装時の負荷より優先する

実装が複雑になっても、使うときのややこしさを削るほうを選んでいる。以下はその方針で決めた。

**単元IDは教師に見せない。** 内部で `u001` のように自動採番する。半角英数のIDを考えるのは実装の都合であって教師の仕事ではない。

**入口は「授業を作る」1つ。** 単元は選ぶか、新しい名前を打つだけ。単元は授業を作る過程で自動的に用意される。「まず単元を作り、次に授業を作る」という2段階を教師に意識させない。

**児童のホームは「きょうの じゅぎょう」1枚だけ。** 教科→単元→授業をたどる操作は「まえの じゅぎょうを さがす」の奥に置いた。ふだんの使用でその階層を見せる必要がない。

**公開の切り替えは一覧から。** 授業直前に、編集画面を開かずに1操作で「今日の授業」にできる。

**道具は頻度で階層化した。** よく使う7つを出し、線・矢印・表・数値・目盛り・プルダウン・原稿用紙は「そのほか」に畳んだ。**消してはいない。** 必要な場面で詰まないよう、1クリックで出る。

**設定パネルは選んだときだけ出す。** 空のパネルを常設すると、場所を取って何も伝えない。

**教師側も下書き中は自動保存する。** ただし公開中は保存しない。授業中に児童の画面が変わってしまうため。ここだけは手で押してもらう。

### 実装した8点

**① もどす・やり直す（Nielsen 3：ユーザーの制御と自由）**
Ctrl+Z / Ctrl+Y。60手前まで。**取り消せることは、確認を求めることより負荷が低い。** 確認ダイアログは毎回の操作に判断を課すが、Undoは失敗したときだけ働く。教師が「試してみる」心理的コストはここで決まる。

**② 自動保存（児童）**
「書いたら押す」を8歳に覚えさせるのは記憶の負担そのもの。打ち終わって2.5秒、または30秒ごと、ページ移動時、戻るとき、に自動保存する。保存状態はヘッダに常時表示（緑＝保存済／黄＝保存中／赤＝未保存）。未保存で離脱しようとすると警告が出る。

**③ 残数表示（Nielsen 1：システム状態の可視性）**
「のこり ◯こ」を常時表示。全部埋まると「ぜんぶ かけました」に変わる。未記入の必須欄には左端に赤い帯が付く。色だけに頼らないよう「*」も残してある。

**④ 直接配置（Normanのマッピング）**
道具を押してから、**置きたいところをドラッグして作る**。以前の「押す→決まった位置に出る→引きずる」の3手が1手になった。Escで取り消し。

**⑤ 近接ツールバー（Fittsの法則）**
選択したオブジェクトのすぐ上に「複製／前面へ／削除」が浮く。右端パネルまでの往復がなくなる。

**⑥ 整列ガイド**
他のオブジェクトの辺・中心、および余白線に8px以内で近づくと吸い付き、朱色のガイド線が出る。縮小表示での目視整列は困難なため。

**⑦ 児童の画面でためす**
編集画面の「児童の画面でためす」を押すと、**児童が実際に使うものと同じ画面**が開く。見た目だけの再現ではないので、入力も、残数の表示も、ページ送りも、縦書きの反映もそのまま試せる。

上に「おためし中です。ここで書いたものは きろくされません」の帯が出て、書いた内容は保存されない。教師自身の行も作られない。「編集にもどる」で戻る。未保存の変更があれば、開く前に保存してから移る。

**⑧ 文字の大きさ**
左右パネルを畳めるようにした（タブ右の「道具」「設定」）。畳むと縮小率が上がり、実効文字サイズが戻る。

### そのほか

- 位置とおおきさは使用頻度が低いので折りたたみに入れた（プログレッシブ・ディスクロージャ）
- 入力欄は塗りと枠で「答えるところ」だと一目で分かるようにしてある（ゲシュタルトの共通領域）
- Ctrl+S、Delete、Escape が効く（Nielsen 4：一貫性と標準）

---

## JSONからの取り込み

### 座標は書かなくてよい

`x, y, w, h` を省略すると自動で並ぶ。`"layout": "2col"` で2段組。`"full": true` を付けた物は全幅になる。

**授業1つ**

```json
{
  "layout": "2col",
  "objects": [
    {"kind": "text", "text": "水のすがたはどう変わるだろう", "size": 32, "full": true},
    {"kind": "in_short", "label": "予想"},
    {"kind": "in_long",  "label": "わかったこと", "required": true},
    {"kind": "in_check", "label": "見えたもの", "options": ["ゆげ","あわ","水てき"]},
    {"kind": "table", "label": "記録", "rows": 4, "cols": 3,
     "heads": ["回","温度","ようす"], "full": true}
  ]
}
```

**単元まるごと**

```json
{
  "unit": {"id": "rika-4-mizu", "subject": "理科", "grade": "4年", "name": "水のすがた"},
  "lessons": [
    {"name": "01 ふしぎを見つける", "layout": "2col", "objects": [ ... ]},
    {"name": "02 予想を立てる",     "objects": [ ... ]}
  ]
}
```

`kind` に使える値は registry.html の定義そのままで、
`text / box / image / line / arrow / table` と
`in_short / in_long / in_number / in_genko / in_radio / in_check / in_select / in_scale`。

### 手順

`?view=import`（教師画面の「取り込み」ボタン）→ 貼る →「中身を確かめる」

日常の授業準備でJSONを触ることはないので、一覧から独立した画面に置いてある。例文のボタンがあるので、まずそれを押して形を見るとよい。

適用前に、ページ数・オブジェクト数・**シートに作られる列の一覧**・警告が出る。ここで列名を確認してから取り込む。単元まるごとなら授業ごとの内訳が出る。

知らない `kind` は飛ばして警告に出す。ラベルが空なら種類名を入れて警告に出す。下にはみ出す物も警告に出るが、勝手にページは増やさない。教師が驚くため。

### 書き出し

授業一覧の「書き出し」でその授業のJSONがコピーされる。「まるごと書き出し」で単元全体。これを渡して直してもらい、また取り込める。**往復が成立する。**

既存の授業名に取り込むと中身が差し替わる。**列の突き合わせはラベル基準なので、ラベルが同じなら児童の回答は引き継がれる。**

---

## 設計上の判断

**授業シートが回答の正本。** JSONを別に持つと二重管理になる。展開列だけを正本にすれば集約という工程が消え、教師がシート上で直接直せる。ただし反映は一方向即時ではない。児童の保存はすぐシートに入るが、**教師がシートで直した内容は、児童が次にその授業を開いたときに反映される**。開いている最中に入れ替わることはない。

**排他制御が無い理由。** 公開時に名簿全員分の行を作るため、保存で行の追加が起きない。児童は自分の行だけを1回の書き込みで更新するので、35人同時でも競合しない。

**列が変わってもデータが消えない。** テンプレート保存時に列を作り直すが、既存データはラベルで突き合わせて引き継ぐ。削除した入力欄の列は `(削除)ラベル` として残す。

**権限フラグを置かない。** オブジェクトの種類そのものが権限を決める。配置物は読み取り専用、入力欄は書き込み可。教師が設定するのは「集約対象かどうか」だけ。

**サーバは種別を知らない。** 複数選択かどうかはエディタがテンプレートに `multi:true` を書き込み、サーバは `o.multi` を見るだけ。種別定義は registry.html に1本化されている。**新しい入力欄を足すときは registry.html の `KIND` に1つ追加すればよく、ツールバー・既定値・描画・設定画面・自動配置・集約列がすべて追随する。**

**ページはA4横（1400×990の論理座標）に固定。** 紙面出力で余白が出ないよう用紙比に合わせてある。

**速さのために削ったもの。**

児童の保存は6往復から3往復にしてある。テンプレートと列見出しをキャッシュし、書く前に読まない。列は「テンプレートの列が先頭」の順で作られるので、テンプレート分の範囲だけを書けば既存値を読まずに済む。

**名簿と教師一覧はキャッシュしない。** 一度はキャッシュしたが、名簿に児童を足しても氏名が古いまま出て、それを直すのに「更新ボタンを押す」手順を覚えさせることになるため戻した。保存が1往復増えるが、押し忘れを前提にした設計より安い。

テンプレートは保存時にキャッシュを破棄するので、教師が紙面を直せばすぐ反映される。反映されないのは、スプレッドシートの `_設定` を直接編集した場合だけ。

児童側は打鍵のたびの走査をまとめてある。残数の表示は0.2秒遅れて更新される。文字サイズの自動縮小は二分探索にしたので、レイアウト再計算が最大40回から7回以内になった。

**GAS特有の対応。** ウェブアプリはサンドボックスiframe内で動くため、`location.search` にパラメータが入らず、`location` を書き換えても遷移しない。パラメータはテンプレート経由で渡し、遷移は `navTo`（`_top`）で行う。

---

## 制約

- テンプレートは `_設定` の1セル。上限50,000字、40,000字で警告
- 複数選択の選択肢にカンマを使えない（シート上で `ア, イ` と保存するため）
- 原稿用紙の書き込みはワークシートの外（画面下の帯）で行う。紙面の中に入力欄を置かないので、レイアウトが崩れない
- 児童は画像を配置できない。写真提出はClassroomに任せる
- Classroom APIは使わない。名簿は貼り付け、成績はClassroom側で手入力
- 手書き描画は非対応

## 未実装（段2以降）

紙面印刷（個票・面付け）／抽出集約の紙面化と画面提示／位置集約／児童による移動・複製・削除／テンプレートの版凍結／単元コピーの自動化

## 最初の検証

1. `runTests` が全31件合格すること（Test.gs）
2. 教師以外のアカウントで `?view=teacher` を開き、拒否されること。**これが通らなければ全児童のデータが誰にでも見える**
3. 児童テスト用アカウントでデプロイURLを開き、権限承認画面が出ずに氏名が表示されること
4. 上のJSON例を取り込み、確認画面に列名が3つ（予想・わかったこと・見えたもの）と表の列が出ること
5. 公開して2アカウントで書き込み、授業シートに即座に反映されること
6. 入力欄を1つ足して保存し、既存データが消えていないこと
